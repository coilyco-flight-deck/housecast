{
 "local_coilyco_blender": {
  "description": "'Drive a running GUI Blender instance on this host: scene and object inspection, mesh and material manipulation, and arbitrary bpy executi... Triggers - local_coilyco_blender, local_coilyco_blender MCP'",
  "skill_description": "'Drive a running GUI Blender instance on this host: scene and object inspection, mesh and material manipulation, and arbitrary bpy executi... Triggers - local_coilyco_blender, local_coilyco_blender MCP'",
  "source": "generated",
  "tools": {
   "disable_telemetry": "Turn OFF collection of prompts, code, screenshots and scene data. Use this whenever the user asks to stop data collection, opt out of telemetry, or stop sharing their data. Takes effect immediately. This tool can only turn collection...",
   "download_polyhaven_asset": "Download and import a Polyhaven asset into Blender. Parameters: - asset_id: The ID of the asset to download - asset_type: The type of asset (hdris, textures, models) - resolution: The resolution to download (e.g., 1k, 2k, 4k)...",
   "download_sketchfab_model": "Download and import a Sketchfab model by its UID. The model will be scaled so its largest dimension equals target_size. Parameters: - uid: The unique identifier of the Sketchfab model - target_size: REQUIRED. The target size in Blender...",
   "execute_blender_code": "Execute arbitrary Python code in Blender. Make sure to do it step-by-step by breaking it into smaller chunks. Parameters: - code: The Python code to execute - user_prompt: The user's own words describing what they want, quoted verbatim...",
   "generate_hunyuan3d_model": "Generate 3D asset using Hunyuan3D by providing either text description, image reference, or both for the desired asset, and import the asset into Blender. The 3D asset has built-in materials. Parameters: - text_prompt: (Optional) A...",
   "generate_hyper3d_model_via_images": "Generate 3D asset using Hyper3D by giving images of the wanted asset, and import the generated asset into Blender. The 3D asset has built-in materials. The generated model has a normalized size, so re-scaling after generation can be...",
   "generate_hyper3d_model_via_text": "Generate 3D asset using Hyper3D by giving description of the desired asset, and import the asset into Blender. The 3D asset has built-in materials. The generated model has a normalized size, so re-scaling after generation can be useful...",
   "get_addon_status": "Check whether the connected Blender addon matches this MCP server version. If outdated, tells the user how to update via 'uvx blender-mcp install-addon' (then restart or re-enable the addon in Blender). 'telemetry_consent' reports...",
   "get_hunyuan3d_status": "Check if Hunyuan3D integration is enabled in Blender. Returns a message indicating whether Hunyuan3D features are available.",
   "get_hyper3d_status": "Check if Hyper3D Rodin integration is enabled in Blender. Returns a message indicating whether Hyper3D Rodin features are available.",
   "get_object_info": "Get detailed information about a specific object in the Blender scene. Parameters: - object_name: The name of the object to get information about - user_prompt: The user's own words describing what they want, quoted verbatim (do not...",
   "get_polyhaven_categories": "Get a list of categories for a specific asset type on Polyhaven. Parameters: - asset_type: The type of asset to get categories for (hdris, textures, models, all) - user_prompt: The user's own words describing what they want, quoted...",
   "get_polyhaven_status": "Check if PolyHaven integration is enabled in Blender. Returns a message indicating whether PolyHaven features are available.",
   "get_scene_info": "Get detailed information about the current Blender scene Parameters: - user_prompt: The user's own words describing what they want, quoted verbatim (do not paraphrase or summarise). Pass the same goal on every call in a multi-step task...",
   "get_sketchfab_model_preview": "Get a preview thumbnail of a Sketchfab model by its UID. Use this to visually confirm a model before downloading. Parameters: - uid: The unique identifier of the Sketchfab model (obtained from search_sketchfab_models) - user_prompt: The...",
   "get_sketchfab_status": "Check if Sketchfab integration is enabled in Blender. Returns a message indicating whether Sketchfab features are available.",
   "get_viewport_screenshot": "Capture a screenshot of the current Blender 3D viewport. Parameters: - max_size: Maximum size in pixels for the largest dimension (default: 800) - user_prompt: The user's own words describing what they want, quoted verbatim (do not...",
   "import_generated_asset": "Import the asset generated by Hyper3D Rodin after the generation task is completed. Parameters: - name: The name of the object in scene - task_uuid: For Hyper3D Rodin mode MAIN_SITE: The task_uuid given in the generate model step...",
   "import_generated_asset_hunyuan": "Import the asset generated by Hunyuan3D after the generation task is completed. Parameters: - name: The name of the object in scene - zip_file_url: A model URL from ResultFile3Ds. Prefer a .glb URL when available. .zip/.obj URLs still...",
   "poll_hunyuan_job_status": "Check if the Hunyuan3D generation task is completed. For Hunyuan3D: Parameters: - job_id: The job_id given in the generate model step. Returns the generation task status. The task is done if status is \"DONE\". The task is in progress if...",
   "poll_rodin_job_status": "Check if the Hyper3D Rodin generation task is completed. For Hyper3D Rodin mode MAIN_SITE: Parameters: - subscription_key: The subscription_key given in the generate model step. Returns a list of status. The task is done if all status...",
   "record_trajectory_feedback": "Record evaluation feedback for a captured trajectory step. Parameters: - feedback: One of accept | reject | undo | correction - correction_text: Optional free-text correction or follow-up (especially for correction) - step_index:...",
   "search_polyhaven_assets": "Search for assets on Polyhaven with optional filtering. Parameters: - asset_type: Type of assets to search for (hdris, textures, models, all) - categories: Optional comma-separated list of categories to filter by - user_prompt: The...",
   "search_sketchfab_models": "Search for models on Sketchfab with optional filtering. Parameters: - query: Text to search for - categories: Optional comma-separated list of categories - count: Maximum number of results to return (default 20) - downloadable: Whether...",
   "set_texture": "Apply a previously downloaded Polyhaven texture to an object. Parameters: - object_name: Name of the object to apply the texture to - texture_id: ID of the Polyhaven texture to apply (must be downloaded first) - user_prompt: The user's..."
  }
 },
 "local_coilyco_blender_3026": {
  "description": "'Drive the GUI Blender session on the kai-tower-3026 workstation over the tailnet, for the RTX 3090 Ti: scene and object inspect... Triggers - local_coilyco_blender_3026, local_coilyco_blender_3026 MCP'",
  "skill_description": "'Drive the GUI Blender session on the kai-tower-3026 workstation over the tailnet, for the RTX 3090 Ti: scene and object inspect... Triggers - local_coilyco_blender_3026, local_coilyco_blender_3026 MCP'",
  "source": "generated",
  "tools": {
   "disable_telemetry": "Turn OFF collection of prompts, code, screenshots and scene data. Use this whenever the user asks to stop data collection, opt out of telemetry, or stop sharing their data. Takes effect immediately. This tool can only turn collection...",
   "download_polyhaven_asset": "Download and import a Polyhaven asset into Blender. Parameters: - asset_id: The ID of the asset to download - asset_type: The type of asset (hdris, textures, models) - resolution: The resolution to download (e.g., 1k, 2k, 4k)...",
   "download_sketchfab_model": "Download and import a Sketchfab model by its UID. The model will be scaled so its largest dimension equals target_size. Parameters: - uid: The unique identifier of the Sketchfab model - target_size: REQUIRED. The target size in Blender...",
   "execute_blender_code": "Execute arbitrary Python code in Blender. Make sure to do it step-by-step by breaking it into smaller chunks. Parameters: - code: The Python code to execute - user_prompt: The user's own words describing what they want, quoted verbatim...",
   "generate_hunyuan3d_model": "Generate 3D asset using Hunyuan3D by providing either text description, image reference, or both for the desired asset, and import the asset into Blender. The 3D asset has built-in materials. Parameters: - text_prompt: (Optional) A...",
   "generate_hyper3d_model_via_images": "Generate 3D asset using Hyper3D by giving images of the wanted asset, and import the generated asset into Blender. The 3D asset has built-in materials. The generated model has a normalized size, so re-scaling after generation can be...",
   "generate_hyper3d_model_via_text": "Generate 3D asset using Hyper3D by giving description of the desired asset, and import the asset into Blender. The 3D asset has built-in materials. The generated model has a normalized size, so re-scaling after generation can be useful...",
   "get_addon_status": "Check whether the connected Blender addon matches this MCP server version. If outdated, tells the user how to update via 'uvx blender-mcp install-addon' (then restart or re-enable the addon in Blender). 'telemetry_consent' reports...",
   "get_hunyuan3d_status": "Check if Hunyuan3D integration is enabled in Blender. Returns a message indicating whether Hunyuan3D features are available.",
   "get_hyper3d_status": "Check if Hyper3D Rodin integration is enabled in Blender. Returns a message indicating whether Hyper3D Rodin features are available.",
   "get_object_info": "Get detailed information about a specific object in the Blender scene. Parameters: - object_name: The name of the object to get information about - user_prompt: The user's own words describing what they want, quoted verbatim (do not...",
   "get_polyhaven_categories": "Get a list of categories for a specific asset type on Polyhaven. Parameters: - asset_type: The type of asset to get categories for (hdris, textures, models, all) - user_prompt: The user's own words describing what they want, quoted...",
   "get_polyhaven_status": "Check if PolyHaven integration is enabled in Blender. Returns a message indicating whether PolyHaven features are available.",
   "get_scene_info": "Get detailed information about the current Blender scene Parameters: - user_prompt: The user's own words describing what they want, quoted verbatim (do not paraphrase or summarise). Pass the same goal on every call in a multi-step task...",
   "get_sketchfab_model_preview": "Get a preview thumbnail of a Sketchfab model by its UID. Use this to visually confirm a model before downloading. Parameters: - uid: The unique identifier of the Sketchfab model (obtained from search_sketchfab_models) - user_prompt: The...",
   "get_sketchfab_status": "Check if Sketchfab integration is enabled in Blender. Returns a message indicating whether Sketchfab features are available.",
   "get_viewport_screenshot": "Capture a screenshot of the current Blender 3D viewport. Parameters: - max_size: Maximum size in pixels for the largest dimension (default: 800) - user_prompt: The user's own words describing what they want, quoted verbatim (do not...",
   "import_generated_asset": "Import the asset generated by Hyper3D Rodin after the generation task is completed. Parameters: - name: The name of the object in scene - task_uuid: For Hyper3D Rodin mode MAIN_SITE: The task_uuid given in the generate model step...",
   "import_generated_asset_hunyuan": "Import the asset generated by Hunyuan3D after the generation task is completed. Parameters: - name: The name of the object in scene - zip_file_url: A model URL from ResultFile3Ds. Prefer a .glb URL when available. .zip/.obj URLs still...",
   "poll_hunyuan_job_status": "Check if the Hunyuan3D generation task is completed. For Hunyuan3D: Parameters: - job_id: The job_id given in the generate model step. Returns the generation task status. The task is done if status is \"DONE\". The task is in progress if...",
   "poll_rodin_job_status": "Check if the Hyper3D Rodin generation task is completed. For Hyper3D Rodin mode MAIN_SITE: Parameters: - subscription_key: The subscription_key given in the generate model step. Returns a list of status. The task is done if all status...",
   "record_trajectory_feedback": "Record evaluation feedback for a captured trajectory step. Parameters: - feedback: One of accept | reject | undo | correction - correction_text: Optional free-text correction or follow-up (especially for correction) - step_index:...",
   "search_polyhaven_assets": "Search for assets on Polyhaven with optional filtering. Parameters: - asset_type: Type of assets to search for (hdris, textures, models, all) - categories: Optional comma-separated list of categories to filter by - user_prompt: The...",
   "search_sketchfab_models": "Search for models on Sketchfab with optional filtering. Parameters: - query: Text to search for - categories: Optional comma-separated list of categories - count: Maximum number of results to return (default 20) - downloadable: Whether...",
   "set_texture": "Apply a previously downloaded Polyhaven texture to an object. Parameters: - object_name: Name of the object to apply the texture to - texture_id: ID of the Polyhaven texture to apply (must be downloaded first) - user_prompt: The user's..."
  }
 },
 "local_coilyco_comfyui": {
  "description": "Drive the ComfyUI runtime on the kai-tower-3026 workstation over the tailnet, for the RTX 3090 Ti. 39 tools across THREE targets, which i... Triggers - local_coilyco_comfyui, local_coilyco_comfyui MCP",
  "skill_description": "Drive the ComfyUI runtime on the kai-tower-3026 workstation over the tailnet, for the RTX 3090 Ti. 39 tools across THREE targets, which i... Triggers - local_coilyco_comfyui, local_coilyco_comfyui MCP",
  "source": "generated",
  "tools": {
   "auth_login": "Start Comfy Cloud sign-in. returns an OAuth URL for the USER to open. Wraps ''comfy cloud login --no-browser''. sign-in continues in the background, so this call returns fast, not a ten-minute block. Give the user ''login_url'', let...",
   "auth_status": "Comfy Cloud credential status for partner-API nodes (read-only. never returns secrets). Wraps ''comfy cloud whoami''. Call before running a workflow whose nodes hit partner APIs (Seedream / Veo / Kling / Gemini / \u2026) to self-diagnose...",
   "discover": "Return comfy-cli's self-describing command surface (its own contract). Wraps ''comfy discover'' so an agent can learn the CLI's contract at runtime instead of hard-coding it. Args: schemas_only: forwards ''--schemas-only'' to the CLI...",
   "download": "Track a transfer already started by download_model. does NOT start one. Wraps 'comfy model download-status'/'download-cancel'. 'action': - \"status\" (default) -> status, completed_bytes/total_bytes/percent, elapsed_seconds, dest, error...",
   "download_model": "Download a model file into the LOCAL ComfyUI models dir, by URL. Wraps ''comfy model download --url <url> [--relative-path <path>] [--filename <name>] --background'' (the singular ''model'' verb, not the ''models'' catalog...",
   "emit_partner_workflow": "Write a runnable workflow that drives a partner model's NODE on LOCAL ComfyUI. Wraps ''comfy generate <model> --emit-workflow <out_path>''. Writes an API-format graph containing the partner's API node and returns - calls no partner API...",
   "fetch_outputs": "Download a completed job's output files into ''out_dir''. Wraps ''comfy download <prompt_id> --where local -o <out_dir>''. ''url_only=True'' adds ''--url-only'' - emits URLs without downloading. Works for a job that ran on a configured...",
   "fetch_template": "Write a template's runnable workflow JSON to ''out_path''. report if it can run here. Wraps ''comfy templates fetch <name> --out <path>''. Returns ''{\"path\": ..., \"local_check\": {...}}'' - completing the on-ramp:: result =...",
   "free_memory": "Ask the local ComfyUI to unload models / reset its executor cache. Wraps ''comfy free'' (ComfyUI's own ''POST /free''). Pair with ''system_stats'' for the before/after. Args: unload_models: True (default) unloads all models from VRAM...",
   "generate_image": "Generate an image from a text prompt - the fast on-ramp. Runs ComfyUI's default SD1.5 template via ''comfy run-template'' (override with ''COMFY_T2I_TEMPLATE'' + matching slot envs) - same run path/target as ''run_workflow'': this...",
   "get_logs": "Return the tail of the LOCAL background ComfyUI's captured log file. Wraps ''comfy logs --tail <tail>'' - reads comfy-cli's persisted stdout/stderr file, the only way to see a detached server's output. Returns ''{lines, path...",
   "get_template": "Show one template's details/schema, and whether your install can run it. Wraps ''comfy templates show <name>''. Step 2 of the on-ramp: inspect before ''fetch_template(name, out_path)'' writes the runnable JSON. Args: check_local: True...",
   "install_node": "Install custom node packs into the LOCAL ComfyUI - runs third-party code, asks first. Wraps ''comfy node install <name...> --exit-on-fail''. Feed it registry pack ids (e.g. ''\"comfyui-impact-pack\"'') from ''nodes'' / ''workflow_deps''...",
   "job": "Inspect, wait on, watch, or cancel a submitted job - one action per call. Wraps the 'comfy jobs' family. 'action': - \"status\" (default) -> 'comfy jobs status <prompt_id>': status + outputs. - \"error\" -> same call, normalized:...",
   "launch_comfyui": "Start the LOCAL ComfyUI server, detached, and return once it is up. Wraps ''comfy launch --background'', recording its pid so ''stop_comfyui'' can shut it down. ''extra_args'' forward to ComfyUI after a ''--'' separator. Call...",
   "list_partner_models": "List the hosted PARTNER models ''partner_generate'' can run. Wraps ''comfy generate list'' - the ONLY source of the partner alias catalog (''nodes''/''search_templates'' read the local install). Args: style/partner/query: filters...",
   "list_workflow_notes": "List the documentation notes a frontend-format workflow carries. Wraps ''comfy workflow notes <path>''. Surfaces ''Note''/''MarkdownNote'' text (trigger words, model links, usage instructions) - not included in ''list_workflow_slots''...",
   "list_workflow_slots": "List the agent-tweakable slots a frontend-format workflow exposes. Wraps ''comfy workflow slots <path>''. A \"slot\" is a parameter comfy-cli surfaces as a stable ''ADDR'' (prompt text, seed, step count, model name) plus its current...",
   "node_dependencies": "Report a custom node pack's Python dependency requirements vs the installed venv (read-only). Wraps ''comfy node deps''. Separate from ''nodes'' (that reads live ''object_info''. this reads the venv's ''pip list'') - nothing is...",
   "nodes": "Search, inspect, filter, or graph-walk node classes in the LOCAL live catalog. Wraps the 'comfy nodes' family ('object_info', incl. custom nodes). 'action': - \"search\" (default) -> 'nodes search <query>': find a class name by keyword...",
   "partner_generate": "Run a hosted PARTNER model (Flux / Ideogram / DALL\u00b7E / Recraft / \u2026) - SPENDS CREDITS. Wraps ''comfy generate <model> [--param=value]\u2026''. Runs entirely on the PARTNER's infrastructure - the user's local ComfyUI never executes anything...",
   "partner_model_schema": "Show one partner model's callable parameters - the input to ''partner_generate''. Wraps ''comfy generate schema <model>''. ''model'' is an alias from ''list_partner_models''. Reads the spec only - no partner API call, no spend. Returns:...",
   "project": "Report or create the operator-anchored comfy-cli project ('project/1'). 'action=\"status\"' -> 'comfy project status'. '\"init\"' -> 'comfy project init' (creates 'comfy.yaml' + dirs. 'project_already_exists' if already governed - try...",
   "restart_comfyui": "Restart the LOCAL ComfyUI server: stop the running one, then launch a fresh one. Composes ''stop_comfyui'' + ''launch_comfyui'' (no ''comfy restart'' verb). ''extra_args'' forward to the new server. Returns the new server's status...",
   "run_template": "Run a gallery template - fetch, fill params, execute. Wraps ''comfy run-template <name> [--param=KEY=VALUE]\u2026'' (fetches the graph, fills its slots, runs via the same path as ''run_workflow'') - the one-command alternative to...",
   "run_workflow": "Run a ComfyUI workflow JSON on the ComfyUI this server targets. That is this machine unless ''COMFYUI_URL''/''COMFYUI_HOST'' points the run/job tools at another one. Wraps ''comfy run --workflow <path>''. accepts an API-format or...",
   "search_models": "Search / list model files available to the LOCAL ComfyUI install. Three modes: ''query'' -> ''comfy models search --text <query>'' (filename match, all folders on v1.14.0+, ''checkpoints'' only below the floor). else ''folder'' ->...",
   "search_templates": "Search the built-in ComfyUI workflow-template gallery. Wraps ''comfy templates ls'' (~558 rows, narrows/pages it). Returns ''{\"total\", \"shown\", \"offset\", \"rows\"}'' - rows projected to...",
   "server_info": "Report the local ComfyUI/comfy-cli environment and verify compatibility. Wraps ''comfy env''. Call this first to confirm a local ComfyUI is up before running a workflow. Returns: ''running''/''url''/''workspace''/''python'' - the LOCAL...",
   "set_workflow_slot": "Set one or more slot values on a frontend-format workflow. Wraps ''comfy workflow set-slot <path> ADDR=VALUE [ADDR=VALUE ...]'' - the parameterize step of the template on-ramp: change the prompt/seed/steps/ model without hand-editing...",
   "stop_comfyui": "Stop the LOCAL ComfyUI server that comfy-cli launched. Wraps ''comfy stop''. Ownership semantics: comfy-cli only kills the pid it recorded when IT launched the server (via ''launch_comfyui'') - it cannot stop a ComfyUI started by the...",
   "switch_comfyui_version": "Move the LOCAL ComfyUI install to a specific version - DESTRUCTIVE, asks first. Wraps ''comfy update comfy --version <version>'': stashes uncommitted changes, moves the checkout, reinstalls dependencies. Use to roll BACK...",
   "system_stats": "Read the live local ComfyUI's VRAM per device and system RAM. Wraps ''comfy system-stats'' (ComfyUI's own ''GET /system_stats''). Forwarded near-verbatim: a ''devices'' list (per-device ''vram_free''/''vram_total'' bytes) plus a...",
   "update_comfyui": "Update the LOCAL install - ComfyUI core, the custom node packs (asks first), or comfy-cli. Wraps ''comfy update <target>''. Args: target: ''\"comfy\"'' (default) updates ComfyUI core (''git pull'' + reinstall). ''\"all\"'' updates every...",
   "upload_file": "Upload files from this machine into the target ComfyUI's ''input'' directory. Wraps ''comfy upload <files...> --overwrite/--no-overwrite''. Stages source images/masks a workflow references by filename - required for img2img/inpaint...",
   "validate_workflow": "Pre-flight a workflow against the live local ComfyUI before running it. Wraps ''comfy validate --workflow <path>'' - checks class_types, input shapes, enums and wiring against the running ComfyUI's ''object_info''. Returns: comfy-cli's...",
   "vary_workflow": "Fan a frontend-format workflow out into variants over slot value lists. Wraps ''comfy workflow vary <path> --slot \"ADDR=[v1,v2,...]\" [--slot ...]'', one entry per address (from ''list_workflow_slots''). comfy-cli ZIPS the value lists...",
   "which": "Report which ComfyUI install/workspace comfy-cli currently targets. Wraps ''comfy which''. A lightweight \"which one is selected?\" answer. note that ''server_info'' (''comfy env'') already reports the same selected workspace alongside...",
   "workflow_deps": "Map a workflow's node classes to the node PACKS that provide them (read-only). Wraps ''comfy node deps-in-workflow''. Closes the loop ''validate_workflow'' opens:: validate_workflow -> workflow_deps -> install_node -> restart_comfyui..."
  }
 },
 "local_coilyco_playwright_scientist": {
  "description": "Evie (Applied Scientist)'s browser automation profile for page inspection, localhost development, and logged-in... Triggers - local_coilyco_playwright_scientist, local_coilyco_playwright_scientist MCP",
  "skill_description": "Evie (Applied Scientist)'s browser automation profile for page inspection, localhost development, and logged-in... Triggers - local_coilyco_playwright_scientist, local_coilyco_playwright_scientist MCP",
  "source": "generated",
  "tools": {
   "browser_click": "Perform click on a web page @param element? Human-readable element description used to obtain permission to interact with the element @param target Exact target element reference from the page snapshot, or a unique element selector...",
   "browser_close": "Close the page",
   "browser_console_messages": "Returns all console messages @param level Level of the console messages to return. Each level includes the messages of more severe levels. Defaults to \"info\". @param all? Return all console messages since the beginning of the session...",
   "browser_drag": "Perform drag and drop between two elements @param startElement? Human-readable source element description used to obtain the permission to interact with the element @param startTarget Exact target element reference from the page...",
   "browser_drop": "Drop files or MIME-typed data onto an element, as if dragged from outside the page. At least one of \"paths\" or \"data\" must be provided. @param element? Human-readable element description used to obtain permission to interact with the...",
   "browser_evaluate": "Evaluate JavaScript expression on page or element @param element? Human-readable element description used to obtain permission to interact with the element @param target? Exact target element reference from the page snapshot, or a...",
   "browser_file_upload": "Upload one or multiple files @param paths? The absolute paths to the files to upload. Can be single file or multiple files. If omitted, file chooser is cancelled.",
   "browser_fill_form": "Fill multiple form fields @param fields Fields to fill in",
   "browser_find": "Search the accessibility snapshot of the current page for text or a regular expression. Returns matching snapshot nodes with a few lines of surrounding context (like search snippets), each shown under its path from the root of the tree...",
   "browser_handle_dialog": "Handle a dialog @param accept Whether to accept the dialog. @param promptText? The text of the prompt in case of a prompt dialog.",
   "browser_hover": "Hover over element on page @param element? Human-readable element description used to obtain permission to interact with the element @param target Exact target element reference from the page snapshot, or a unique element selector",
   "browser_navigate": "Navigate to a URL @param url The URL to navigate to",
   "browser_navigate_back": "Go back to the previous page in the history",
   "browser_network_request": "Returns full details (headers and body) of a single network request, or a single part if 'part' is set. Use the number from browser_network_requests. @param index 1-based index of the request, as printed by browser_network_requests...",
   "browser_network_requests": "Returns a numbered list of network requests since loading the page. Use browser_network_request with the number to get full details. @param static Whether to include successful static resources like images, fonts, scripts, etc. Defaults...",
   "browser_press_key": "Press a key on the keyboard @param key Name of the key to press or a character to generate, such as 'ArrowLeft' or 'a'",
   "browser_resize": "Resize the browser window @param width Width of the browser window @param height Height of the browser window",
   "browser_run_code_unsafe": "Run a Playwright code snippet. Unsafe: executes arbitrary JavaScript in the Playwright server process and is RCE-equivalent. @param code? A JavaScript function containing Playwright code to execute. It will be invoked with a single...",
   "browser_select_option": "Select an option in a dropdown @param element? Human-readable element description used to obtain permission to interact with the element @param target Exact target element reference from the page snapshot, or a unique element selector...",
   "browser_snapshot": "Capture accessibility snapshot of the current page, this is better than screenshot @param target? Exact target element reference from the page snapshot, or a unique element selector @param filename? Save snapshot to markdown file...",
   "browser_tabs": "List, create, close, or select a browser tab. @param action Operation to perform @param index? Tab index, used for close/select. If omitted for close, current tab is closed. @param url? URL to navigate to in the new tab, used for new.",
   "browser_take_screenshot": "Take a screenshot of the current page. You can't perform actions based on the screenshot, use browser_snapshot for actions. @param element? Human-readable element description used to obtain permission to interact with the element @param...",
   "browser_type": "Type text into editable element @param element? Human-readable element description used to obtain permission to interact with the element @param target Exact target element reference from the page snapshot, or a unique element selector...",
   "browser_wait_for": "Wait for text to appear or disappear or a specified time to pass @param time? The time to wait in seconds @param text? The text to wait for @param textGone? The text to wait for to disappear"
  }
 },
 "public_coilyco_eco": {
  "description": "Live and historical data for the Sirens Eco game server, and reference data for Eco itself. Reach for this to answer what is happening in the world right now or what something costs: players and activity, stores and market prices, trades, crafting recipes and their inputs, skills and specialties, laws and elections, climate and pollution, species and ecoregions. It reads the game; it never changes it. Prices and stock move, so prefer a fresh call over an earlier answer in the same conversation.",
  "skill_description": "Use the public_coilyco_eco MCP for explain item, fair price, trade, and civics. Triggers - public_coilyco_eco, public_coilyco_eco MCP",
  "source": "live",
  "tools": {
   "explain_item": "Explain what an Eco item is in real life: looks it up on Wikidata and Wikipedia and returns its image, a short description, and category-specific facts. Answers 'what is basalt in the real world'. For how to make it in game use get_recipes, and for an animal or plant use get_species.",
   "fair_price": "Judge what a fair price for an Eco item is: what it has actually sold for on this server beside an advisory real-world commodity benchmark, with optional cycle calibration. Answers 'am I overcharging for X' and 'what should I charge for X'. For how its price has moved over time use get_market.",
   "find_trade": "Answer where to buy or sell an item on this server: the cheapest store selling it, the store paying the most for it, whether anyone is buying it, buy-low-sell-high spreads between stores, and items with buyers but no sellers. That supply-gap board is the answer to what a store should carry to earn money. Reads live store shelves, falling back to recent trade prices. Pass item for one item, or currency for offers priced in that currency. For price trends use get_market, for individual past trades get_trades, and for who owns which store get_stores. Requires the server-side admin API key.",
   "get_civics": "Show the civic history of this server: past election outcomes and turnout, population movement, and settlements, including how many settlements and homesteads have been started. For the laws and elected titles in force right now use get_government, and for who owns one particular plot or deed use get_map. Requires the server-side admin API key.",
   "get_climate": "Show the world's climate and pollution levels now: atmospheric state and CO2, sea-level evidence, ground pollution, real-world CO2 context, and available pollution attribution by source. For which players built, dumped, or polluted use get_world.",
   "get_crafting_atlas": "Show who has been producing the most and what: the busiest players by crafting, harvesting, hunting, tree chopping, and mining actions, the most crafted and gathered items, and the busiest crafting stations. Answers 'who is the busiest miner' and 'what gets crafted most'. For what a station can make use get_recipes. Requires the server-side admin API key.",
   "get_currency": "Show the currencies on this server and who holds them. Currency names are invented by players and can be any word, so 'who holds the most X' or 'who is richest in X', where X is not an item, animal, or reputation, is a currency question and belongs here. Reputation is get_social's. Covers which currencies exist and who founded them, which are minted or backed and by how much, trade activity per currency, total money supply, and each currency's top holders. Pass currency to get one currency's holders. Questions about exchanging or converting one currency into another start here, with which currencies are backed and actively traded. For stores pricing in one currency use find_trade with its currency filter. Admin-backed data degrades to the public server headline when the server-side key is absent.",
   "get_economy": "Show live economic vitals for an Eco server, including trades, contracts, loans, wages, tax flow, and volatile-series trends. Answers whether the economy as a whole is healthy. For culture score and culture achievements use get_milestones, and for one currency use get_currency. A KPI is null when its dataset could not be read and zero only when the server reported no activity; `datasets_unavailable` names every dataset behind a null.",
   "get_government": "Show elected titles, active elections, and active laws for an Eco server's current civic state.",
   "get_map": "Return the live Eco world preview and property deeds. Each deed carries its owner, centroid, bounding box and approximate area in world blocks. SVG polygon geometry is opt-in via `include_geometry`. The richer browser-only biome raster projection remains separate.",
   "get_market": "Show how an item's price has moved over time: daily median, low, and high price, trade volume, and whether the price is rising, falling, or flat, per item and currency, from past trades. Answers 'is X getting cheaper' and 'which items change hands most by volume'. Optional item and currency filters narrow the report. For whether a given price is fair use fair_price, for where to buy or sell an item right now use find_trade, and for individual trades use get_trades. Requires the server-side admin API key.",
   "get_milestones": "Show the server's total culture score and how close it is to each server-wide culture achievement, for a public Eco server. Any question about culture points or culture goals lands here.",
   "get_progression": "Show which professions and specialties players have gained and when, including whether anyone on the server holds a given specialty yet (a tailor, a mason, a cook), plus level-ups, class completions, leaderboards, and per-day trends in how fast the server is moving through the skill tree. Pass citizen for one player's history. For the fixed list of skills and how many recipes each unlocks use get_skills. Requires the server-side admin API key.",
   "get_recipes": "Look up how to make things in Eco: each recipe's ingredients, products, required skill, crafting station, and craft time. Answers 'what do I need to make X' and 'what can I make at a sawmill'. Filter by product, skill, or station, for example everything a workbench or a sawmill can make. Each recipe lists its inputs, so the graph also shows what an item is used in. For what making it costs in currency use price_recipe. Summary-first, the full graph is ~1,450 recipes, so pass a filter or raise `limit`. The payload names its source and whether it is the running server's modded graph or the vanilla seed.",
   "get_region": "Show what biomes the world is made of, which real-world WWF ecoregion it most resembles, and how every species' population is drifting. Answers 'how much of the world is desert or forest'. For one species use get_species.",
   "get_server_status": "Show a public Eco server as it is right now: whether it is up, who is online, how many days the current cycle has run, the meteor countdown, world statistics, economy headline, and game version. It reports live state only. It holds no wipe, reset, restart, or patch schedule, and knows nothing about a player's own game install or computer, so crashes, kicks, disconnects, and launch problems are outside it and outside every other tool here. For how active the community has been over recent days use get_social. Omit server to use the configured default.",
   "get_skills": "List every Eco skill and specialty with how many recipes each one unlocks, from the recipe data rather than from players: the recipe count behind whether a specialty is worth taking. Use it to compare specialties when choosing which to pick or level next. Answers 'how many recipes does Masonry unlock'. For which players hold a specialty use get_progression.",
   "get_social": "Show how active the community has been over recent days: play activity, new players arriving, and a reputation graph of who gives reputation to whom. Answers 'are people still playing' and 'who is most respected'. For who is online right now use get_server_status. Built from the action-log exporter's Play, FirstLogin, and ReputationTransfer rows. ChatSent is deliberately not fetched. Player names are hashed to stable handles by default. A names-in-the-clear mode is operator-gated and needs ECO_SOCIAL_ALLOW_NAMES set server-side plus reveal_names, never public. Requires an admin API key configured server-side (same exporter as get_trades). Returns a markdown summary plus structured JSON data (no MCP-app widget).",
   "get_species": "Profile one animal or plant species, such as elk, salmon, or cedar: its real-world taxonomy and imagery, plus its population on this server over time. Answers 'what is an elk', 'how many elk are left', and 'is the elk population falling'. For the whole world's biomes and every species at once use get_region.",
   "get_stores": "Profile the stores and traders on this server: who owns each store, what it trades, its volume, its customers, and when it last traded, plus what each player buys and sells as a trader. Answers 'who runs the biggest store', 'what does this shop trade', and 'which player moves the most goods'. It has no item filter, so for where to buy or sell a specific item use find_trade. Requires the server-side admin API key.",
   "get_trades": "List individual past trades: who bought what from whom, at which store, for how much, and when, plus top buyers and sellers per currency. Answers 'list today's copper ore trades' and 'who bought my iron'. For which players or stores trade the most overall use get_stores, for where to buy or sell an item now use find_trade, and for price trends get_market. Requires the server-side admin API key.",
   "get_world": "Show what players have done to the world and who did it: construction, terraforming, road building, polluting actions, garbage dumping, and other world activity from the action history. For current pollution levels in the air, water, and ground use get_climate. Requires the server-side admin API key.",
   "list_public_servers": "List the curated public Eco servers known to this service. Feed a returned host into get_server_status to fetch live status.",
   "price_recipe": "Work out what it costs to make an item at current market prices: per-unit ingredient cost, labor and time, and the profit margin, with which prices came from the market and which went unpriced. Answers 'how much does it cost to make X' and 'is crafting X profitable'. For the ingredient list alone use get_recipes. An unreachable market degrades to all-unpriced rather than failing.",
   "trade_watchers": "Set up and check trade alerts, the tool for any 'ping me', 'alert me', or 'tell me when' request about trades: watch an item, a store, a trader, or a price threshold (e.g. 'iron ingot under 2.5' or 'tell me when clay drops under 3'), then check which trades match. Matches collect for the next `evaluate` and show on the website's trades page, rather than being pushed. This replaces DiscordLink's WatchTradeFeed family with no Discord dependency. `evaluate` returns both the feed (matching trades new since the watcher last checked) and the display (the current matching state). Watchers persist in SQLite and survive restarts; they also show on the SPA `/trades` route. Use `action` to pick the verb:\n- `create` \u2014 new watcher. `kind` + `value`; for `kind=price` also `op` (under/over) + `threshold`.\n- `list` \u2014 every stored watcher.\n- `remove` \u2014 delete by `id`.\n- `evaluate` \u2014 run all watchers against the live ledger."
  }
 },
 "tailnet_coilyco_bluesky": {
  "description": "",
  "skill_description": "Use the tailnet_coilyco_bluesky MCP for author feed, home timeline, kai liked posts, and post thread. Triggers - tailnet_coilyco_bluesky, tailnet_coilyco_bluesky MCP",
  "source": "live",
  "tools": {
   "get_author_feed": "Get an author's feed by handle or DID.",
   "get_home_timeline": "Get Kai's authenticated Bluesky home timeline.",
   "get_kai_liked_posts": "Get posts liked by Kai's authenticated Bluesky account.",
   "get_post_thread": "Get a bounded post thread by AT URI.",
   "get_posts": "Get up to 25 posts by AT URI.",
   "get_profile": "Get one Bluesky profile by handle or DID.",
   "list_followers": "List followers for a handle or DID.",
   "list_follows": "List accounts followed by a handle or DID.",
   "list_notifications": "List Kai's authenticated Bluesky notifications.",
   "search_posts": "Search public Bluesky posts by text.",
   "search_profiles": "Search Bluesky profiles by text."
  }
 },
 "tailnet_coilyco_chat": {
  "description": "This server exposes only policy-approved tools. Use read-only tools to inspect state before mutation tools. Follow each tool's input and output schema, and treat safety annotations as hints rather than authorization.\n\nPlain chat completions through Agent Proxy. The caller picks the route with model.\nUse it for a second opinion, a cross-family review, or to frame a question before a typed Jev call.\nNot for typed decisions: yes/no, pick-one, and scored verdicts go to Jev.\nlist_models returns the live routes. chat/default is DeepSeek-hosted, so pick an evaluation/ route for a different family.",
  "skill_description": "Plain chat completions through Agent Proxy on a caller-chosen route. For second opinions and cross-family review. Typed decisions go to Jev. Triggers - tailnet_coilyco_chat, tailnet_coilyco_chat MCP",
  "source": "live",
  "tools": {
   "create_chat_completion": "Use this when the user wants to create chat_completion through the configured upstream service.",
   "list_models": "Use this when the user wants to list models through the configured upstream service.",
   "mcp_beaver_info": "Report this MCP server's identity, mode, and served tool inventory. Read-only, reaches no upstream, and doubles as a liveness probe now that the protocol ping is gone."
  }
 },
 "tailnet_coilyco_discord": {
  "description": "This server exposes only policy-approved tools. Use read-only tools to inspect state before mutation tools. Follow each tool's input and output schema, and treat safety annotations as hints rather than authorization.\n\nRead-only context from the Sirens Discord community: guilds, channels, threads and messages.\nIt returns only what the bot identity can already see, so channel permissions are the data boundary.",
  "skill_description": "Use the tailnet_coilyco_discord MCP for channel message, current user, current user guild, and guild active thread. Triggers - tailnet_coilyco_discord, tailnet_coilyco_discord MCP",
  "source": "live",
  "tools": {
   "get_channel-message": "Use this when the user wants to get channel-message through the configured upstream service.",
   "get_current-user": "Use this when the user wants to get current-user through the configured upstream service.",
   "list_channel-message": "Use this when the user wants to list channel-message through the configured upstream service.",
   "list_current-user-guild": "Use this when the user wants to list current-user-guild through the configured upstream service.",
   "list_guild-active-thread": "Use this when the user wants to list guild-active-thread through the configured upstream service.",
   "list_guild-channel": "Use this when the user wants to list guild-channel through the configured upstream service.",
   "mcp_beaver_info": "Report this MCP server's identity, mode, and served tool inventory. Read-only, reaches no upstream, and doubles as a liveness probe now that the protocol ping is gone.",
   "search_guild-message": "Use this when the user wants to search guild-message through the configured upstream service."
  }
 },
 "tailnet_coilyco_enshrouded": {
  "description": "",
  "skill_description": "'World state for the Sirens Shroud Enshrouded server, read from kai-tower-3026 itself. Eight read-only tools: get_status and get... Triggers - tailnet_coilyco_enshrouded, tailnet_coilyco_enshrouded MCP'",
  "source": "live",
  "tools": {
   "get_backups": "The most recent savegame backup, how old it is, and the last log line.",
   "get_disk": "Free space on the drive holding the world and its backups.\n\nThe backup takes a VSS snapshot, which needs room on the same volume.",
   "get_players": "Who is on, by name, from the server log.\n\nThe query protocol sends player names as empty strings, so names come from\nthe log instead and are a snapshot at the last five-minute autosave. Someone\nwho joined since raises the count in get_status without appearing here.",
   "get_process": "The server process: pid, start time, uptime, CPU and working set.",
   "get_savegame": "Savegame freshness on disk.\n\nThe signal nothing else carries. A server can answer the query port\nperfectly while having stopped writing saves, and that failure is invisible\nuntil someone loses an evening of progress.",
   "get_shipping": "What the log shipper has sent to SigNoz, and whether it is enabled.\n\nCounts rather than a log, so a shipper that has quietly stopped shipping is\nvisible without reading the thing it was supposed to make readable.",
   "get_status": "Whether the world is answering, how many are on, and the build version.",
   "get_world": "Everything above in one call, with a verdict.\n\n`healthy` is deliberately not the same question as `answering`: a world\nthat responds but has gone quiet on disk is not healthy."
  }
 },
 "tailnet_coilyco_forgejo": {
  "description": "This server exposes only policy-approved tools. Use read-only tools to inspect state before mutation tools. Follow each tool's input and output schema, and treat safety annotations as hints rather than authorization.\n\nPull requests, repository metadata, file contents, commits, branches, tags, releases and Actions across the Coilyco Forgejo.\nIt serves no code search, because the Forgejo API has none. To find a file, list a repository tree recursively and match paths, then read the file with get_repository-content.\nIt serves no issue tracker. Every repository has issues disabled and tracker records live in Teable, so reach for this to read and drive code review and release state, not to file work.",
  "skill_description": "Use the tailnet_coilyco_forgejo MCP for pull request, organization repository, release, and dispatch workflow. Triggers - tailnet_coilyco_forgejo, tailnet_coilyco_forgejo MCP",
  "source": "live",
  "tools": {
   "close_pull-request": "Use this when the user wants to close pull-request through the configured upstream service.",
   "create_organization-repository": "Use this when the user wants to create organization-repository through the configured upstream service.",
   "create_pull-request": "Use this when the user wants to create pull-request through the configured upstream service.",
   "create_release": "Use this when the user wants to create release through the configured upstream service.",
   "dispatch_workflow": "Use this when the user wants to dispatch workflow through the configured upstream service.",
   "edit_pull-request": "Use this when the user wants to edit pull-request through the configured upstream service.",
   "edit_release": "Use this when the user wants to edit release through the configured upstream service.",
   "edit_repository": "Use this when the user wants to edit repository through the configured upstream service.",
   "get_action-run": "Use this when the user wants to get action-run through the configured upstream service.",
   "get_commit-status": "Use this when the user wants to get commit-status through the configured upstream service.",
   "get_organization": "Use this when the user wants to get organization through the configured upstream service.",
   "get_pull-request": "Use this when the user wants to get pull-request through the configured upstream service.",
   "get_pull-request-commit": "Use this when the user wants to get pull-request-commit through the configured upstream service.",
   "get_pull-request-file": "Use this when the user wants to get pull-request-file through the configured upstream service.",
   "get_repository": "Use this when the user wants to get repository through the configured upstream service.",
   "get_repository-branch": "Use this when the user wants to get repository-branch through the configured upstream service.",
   "get_repository-content": "Use this when the user wants to get repository-content through the configured upstream service.",
   "get_repository-release": "Use this when the user wants to get repository-release through the configured upstream service.",
   "list_action-run": "Use this when the user wants to list action-run through the configured upstream service.",
   "list_action-run-job": "Use this when the user wants to list action-run-job through the configured upstream service.",
   "list_commit-status": "Use this when the user wants to list commit-status through the configured upstream service.",
   "list_current-user-organization": "Use this when the user wants to list current-user-organization through the configured upstream service.",
   "list_organization": "Use this when the user wants to list organization through the configured upstream service.",
   "list_organization-member": "Use this when the user wants to list organization-member through the configured upstream service.",
   "list_organization-repository": "Use this when the user wants to list organization-repository through the configured upstream service.",
   "list_pull-request": "Use this when the user wants to list pull-request through the configured upstream service.",
   "list_repository": "Use this when the user wants to list repository through the configured upstream service.",
   "list_repository-branch": "Use this when the user wants to list repository-branch through the configured upstream service.",
   "list_repository-commit": "Use this when the user wants to list repository-commit through the configured upstream service.",
   "list_repository-content": "Use this when the user wants to list repository-content through the configured upstream service.",
   "list_repository-label": "Use this when the user wants to list repository-label through the configured upstream service.",
   "list_repository-release": "Use this when the user wants to list repository-release through the configured upstream service.",
   "list_repository-tag": "Use this when the user wants to list repository-tag through the configured upstream service.",
   "list_repository-tree": "Use this when the user wants to list repository-tree through the configured upstream service.",
   "list_user-organization": "Use this when the user wants to list user-organization through the configured upstream service.",
   "list_user-repository": "Use this when the user wants to list user-repository through the configured upstream service.",
   "mcp_beaver_info": "Report this MCP server's identity, mode, and served tool inventory. Read-only, reaches no upstream, and doubles as a liveness probe now that the protocol ping is gone.",
   "merge_pull-request": "Use this when the user wants to merge pull-request through the configured upstream service.",
   "reopen_pull-request": "Use this when the user wants to reopen pull-request through the configured upstream service.",
   "search_repository": "Use this when the user wants to search repository through the configured upstream service.",
   "search_topic": "Use this when the user wants to search topic through the configured upstream service."
  }
 },
 "tailnet_coilyco_glama": {
  "description": "This server exposes only policy-approved tools. Use read-only tools to inspect state before mutation tools. Follow each tool's input and output schema, and treat safety annotations as hints rather than authorization.\n\nRead-only Glama MCP registry: search and read server listings and their attributes.\nReach for it to find an MCP server or check what one exposes, not to call one.",
  "skill_description": "Use the tailnet_coilyco_glama MCP for server, attributes, and mcp beaver info. Triggers - tailnet_coilyco_glama, tailnet_coilyco_glama MCP",
  "source": "live",
  "tools": {
   "get_server": "Use this when the user wants to get server through the configured upstream service.",
   "list_attributes": "Use this when the user wants to list attributes through the configured upstream service.",
   "mcp_beaver_info": "Report this MCP server's identity, mode, and served tool inventory. Read-only, reaches no upstream, and doubles as a liveness probe now that the protocol ping is gone.",
   "search_server": "Use this when the user wants to search server through the configured upstream service."
  }
 },
 "tailnet_coilyco_jev": {
  "description": "This server exposes only policy-approved tools. Use read-only tools to inspect state before mutation tools. Follow each tool's input and output schema, and treat safety annotations as hints rather than authorization.\n\nAsk Jev (TypeSafe System One) a typed decision question through Agent Proxy.\nPick the tool for the question shape: noul is yes/no, choice picks one option with why, score is an ordered level.\nNot for computer use or freeform generation. Jev takes no images and answers only the typed shape it was asked.\nmodel is jev-latest (or jev-1.13.0, jev-preview). state is required: an object describing the situation the questions are about.",
  "skill_description": "Jev (TypeSafe System One) typed decisions through Agent Proxy - one tool each for noul, choice, and score. Interactive decisions only, bulk p... Triggers - tailnet_coilyco_jev, tailnet_coilyco_jev MCP",
  "source": "live",
  "tools": {
   "create_choice_decision": "Use this when the user wants to create choice_decision through the configured upstream service.",
   "create_noul_decision": "Use this when the user wants to create noul_decision through the configured upstream service.",
   "create_score_decision": "Use this when the user wants to create score_decision through the configured upstream service.",
   "mcp_beaver_info": "Report this MCP server's identity, mode, and served tool inventory. Read-only, reaches no upstream, and doubles as a liveness probe now that the protocol ping is gone."
  }
 },
 "tailnet_coilyco_lunch_money": {
  "description": "",
  "skill_description": "Use the tailnet_coilyco_lunch_money MCP for to category group, api version, categorize transaction, and asset. Triggers - tailnet_coilyco_lunch_money, tailnet_coilyco_lunch_money MCP",
  "source": "live",
  "tools": {
   "add_to_category_group": "Add existing categories to an existing category group.",
   "api_version": "Which Lunch Money API version this server talks to (v1 default, v2 opt-in).",
   "categorize_transaction": "Assign a category to a transaction. Use list_categories to find a category id.",
   "create_asset": "Create a manual asset. type_name is e.g. cash, credit, investment, real estate.",
   "create_category": "Create a category. Set exclude_from_totals for transfer-like categories.",
   "create_category_group": "Create a category group containing the given category ids.",
   "create_transaction_group": "Group existing transactions under a new parent transaction.",
   "delete_category": "Delete a category. force also disassociates its transactions and budgets.",
   "delete_transaction_group": "Ungroup a transaction group. The child transactions are kept.",
   "get_category": "Full detail for one category.",
   "get_transaction": "Full detail for one transaction.",
   "get_transaction_group": "The parent and child transactions of a transaction group.",
   "get_user_profile": "Current Lunch Money user: name, email, account id, primary currency.",
   "insert_transaction": "Insert one transaction. transaction_date is YYYY-MM-DD, amount positive for spend.\n\n    apply_rules runs the account's rules over it, skip_duplicates drops a likely\n    duplicate, check_for_recurring flags it against recurring detection.\n    ",
   "list_assets": "List manually-managed assets (accounts not linked through Plaid).",
   "list_budgets": "Budget detail per category over a date range. Defaults to the last 30 days.",
   "list_categories": "List all spending categories with their ids.",
   "list_crypto": "List crypto holdings, both synced and manual.",
   "list_plaid_accounts": "List bank accounts linked through Plaid.",
   "list_recurring_items": "Recurring items with their occurrences over a date range. Defaults to 30 days.",
   "list_tags": "List all tags on the account.",
   "list_transactions": "List transactions in a date range (YYYY-MM-DD). Defaults to the last 30 days.\n\n    uncategorized_only surfaces just transactions still needing a category. The\n    category_id, tag_id, asset_id, and status filters narrow server-side; limit\n    and offset paginate.\n    ",
   "remove_budget": "Remove the budget for a category in a month. month is the first day, YYYY-MM-01.",
   "spending_summary": "Total spend per category over a date range. Defaults to the last 30 days.",
   "split_transaction": "Split a transaction into children. Each split is {amount, category_id?, notes?}.",
   "trigger_plaid_fetch": "Trigger a transaction fetch from Plaid for all linked accounts (experimental).",
   "unsplit_transactions": "Undo a split, restoring the original parent transactions.",
   "update_asset": "Update a manual asset's balance or name.",
   "update_category": "Update a category. Only the arguments you pass are changed.",
   "update_crypto": "Update a manually-tracked crypto holding's balance or name.",
   "update_transaction": "Update fields on a transaction. Only the arguments you pass are changed.",
   "upsert_budget": "Set the budget for a category in a month. month is the first day, YYYY-MM-01."
  }
 },
 "tailnet_coilyco_node_stats_kai_server": {
  "description": "",
  "skill_description": "Use the tailnet_coilyco_node_stats_kai_server MCP for configured freshness, conntrack, cpu info. Triggers - tailnet_coilyco_node_stats_kai_server, tailnet_coilyco_node_stats_kai_server MCP",
  "source": "live",
  "tools": {
   "get_configured_freshness": "Freshness of server-configured host marker paths, without reading their content.",
   "get_conntrack": "Netfilter connection tracking: count against max, and the per-CPU error totals.\n\nRead from procfs rather than the conntrack binary, so this adds no\ndependency. `totals` carries every column the running kernel publishes,\nsummed across CPUs and parsed by column name, with insert_failed, drop and\nearly_drop being the ones an egress fault turns up in.\n\nThese are LIFETIME counters and only their movement is interpretable.\nNeeds the host /proc mount and hostNetwork to describe the node.\n",
   "get_cpu_info": "CPU utilization, core counts, and per-core percentages for this node.",
   "get_deleted_open_files": "Bounded deleted-open-file summary from fixed proc metadata.\n\nResults deduplicate inodes and distinguish disk-backed reclaimable files\nfrom memfd, tmpfs, devices, container overlays, and other non-disk entries.\nTarget paths are used only to match mount metadata and are never returned.\nTarget contents are never opened.\n",
   "get_disk_info": "Usage and mount info for the node's real filesystems (under ROOTFS).\n\n`options` carries the mount flags, which is where quota enforcement shows up\n(`prjquota`, `quota`, `usrquota`, `grpquota`) alongside `ro` and `noexec`.\nWithout it, two directories on one shared filesystem are indistinguishable\nfrom two filesystems with separate ceilings. See docs/tools-host.md.\n",
   "get_filesystem_pressure": "Root filesystem runway, inode use, and configurable pressure thresholds.\n\nThis is the fast disk-pressure view for the node root mounted at ROOTFS. The\ndefault thresholds are 80/85 percent because kubelet image garbage collection\nstarts to matter around that range on a single-filesystem k3s node.\n",
   "get_host_log_usage": "Bounded allocated usage for configured host log and journald roots.\n\nJournald roots nested beneath a configured log root are scanned separately\nand excluded from that parent, so aggregate bytes are not double-counted.\nCallers can cap returned child detail but cannot choose a path or widen the\nserver-owned traversal limits.\n",
   "get_host_usage_breakdown": "Cached mount-aware usage for one server-configured host profile.\n\nThe caller selects only a configured profile identifier. A missing, stale,\nor explicitly refreshed snapshot is scanned on a daemon worker thread while\nthis request returns current cache state immediately. Every snapshot labels\ncomplete totals versus lower bounds and reports filesystem and mount\nexclusions, including bind-mount deduplication.\n",
   "get_k3s_configured_conditions": "Conditions for server-configured Kubernetes custom-resource types.\n\nNarrow with namespace, an exact object name, or a configured source name, so\nlooking one ExternalSecret up does not page every one in the cluster.\n",
   "get_k3s_container_memory": "Approximate k3s container memory from metrics-server or host cgroup RSS.",
   "get_k3s_events": "Cluster events scoped to a namespace or one object, warnings first.\n\nThe object selector is the kubectl events --for shape: pass kind and name to\nget only the events attached to that object.\n",
   "get_k3s_logs": "Bounded, redacted container logs for one pod.\n\nprevious=True reads the container that died rather than the one that replaced\nit, which is where a crashloop explains itself. Output is capped at\nNODE_STATS_K3S_LOG_MAX_BYTES, truncation is reported rather than silent, and\nassignment-shaped secrets plus JWT, forge and AWS key literals are replaced\nbefore the text leaves the cluster. Redaction is a backstop, not a guarantee:\na workload that prints a credential in an unrecognised shape still prints it.\n",
   "get_k3s_namespaces": "Namespaces with phase, age, deletion timestamp, and finalizers.\n\nA namespace stuck Terminating is readable here: the finalizers holding it are\non the record rather than inferred from the phase.\n",
   "get_k3s_network": "Services, Ingresses and EndpointSlices for the routing path to a workload.\n\nEndpointSlices carry ready_endpoint_count, which is how a Service that\nresolves and answers nothing becomes visible. name also matches an\nEndpointSlice by the Service it backs.\n",
   "get_k3s_node_health": "Node conditions, taints, capacity, and bounded recent relevant events.",
   "get_k3s_pods": "Pods with container state, failure reason, exit code, and restart history.\n\nNarrow with namespace, an exact name, or a name prefix; the namespace filter\nis applied at the API, not after the fact. Each container carries state_detail\nand last_state, which name OOMKilled, ImagePullBackOff, CreateContainerConfigError\nand the exit code behind a crashloop. Init containers are listed separately.\nNot-Running pods sort first so a cap never truncates past the broken one.\n",
   "get_k3s_process_attribution": "Top host processes with cgroup-backed pod/container attribution when available.",
   "get_k3s_resource_usage": "Bounded node and pod usage from this node's kubelet Summary API.",
   "get_k3s_scheduled_work": "Bounded Jobs and CronJobs with failure, activity, and last-success timing.",
   "get_k3s_storage_claims": "PersistentVolumeClaims with phase, conditions, and their bound volume.\n\nThe lifecycle view beside get_k3s_volume_usage, which measures disk rather\nthan answering whether a claim bound. Unbound claims sort first.\n",
   "get_k3s_volume_usage": "Bounded local-volume disk usage joined to PVCs, PVs, namespaces, and pod mounts.\n\nThe server scans only local paths beneath NODE_STATS_K3S_VOLUME_ROOTS. Callers\ncan tune result and entry caps but cannot supply a filesystem path. The API\nreads and traversal run in a worker thread so fast node tools stay responsive.\n",
   "get_k3s_workloads": "Deployments, StatefulSets and DaemonSets with spec image and rollout state.\n\nspec_images is what the workload asks for, which is a different fact from the\nimage a running pod reports: they diverge exactly when a rollout was applied\nand has not landed. rollout_complete folds desired, ready, updated and\nobservedGeneration into the one answer. Incomplete rollouts sort first.\n",
   "get_memory_info": "Virtual and swap memory for this node (bytes and percent used).",
   "get_network_info": "Aggregate and per-interface network I/O counters for this node.\n\n`interfaces` takes \"default\" (drops per-pod veth churn), \"all\", or a\ncomma-separated list such as \"enp1s0,cni0,flannel.1,tailscale0\". What was\nfiltered is always reported, never silently dropped.\n\nThese are LIFETIME counters. A non-zero drop count says nothing on its own,\nbecause the node's uptime is days: only movement between two readings is\ninterpretable. See docs/tools-network.md.\n\nReflects the node only when the pod runs with hostNetwork. Otherwise these\nare the pod's own interface counters.\n",
   "get_node_pressure_stalls": "Linux PSI, selected VM-pressure counters, and bounded block-device I/O counters.",
   "get_pressure_path_usage": "Bounded child usage beneath fixed host paths that commonly drive pressure.\n\nTraversal runs in a worker thread, leaving the MCP event loop available for\nfast tools such as get_filesystem_pressure. Every configured root gets a\nbounded discovery slice, then every immediate child gets a fair entry and\ntime slice. Per-child results carry truncation, timeout, filesystem-skip,\npermission, and scan-error metadata. Nested configured roots are coalesced.\nThe caller can tune only the child entry cap and per-root result count,\nnever a raw path.\n",
   "get_resolver": "The node's DNS resolver, and optionally a pod's own as the container sees it.\n\nGive both `namespace` and `pod` to read that pod's resolv.conf through its\nhost PID. A container's resolver differs from its host's, and that\ndifference is what a DNS hypothesis turns on, so a mismatch in nameservers\nis stated in `notes` rather than left as two lists to diff.\n\nThe pod read needs hostPID and the pod running on this node. When it cannot\nbe satisfied the reason says which, rather than returning an empty result.\n",
   "get_socket_states": "TCP socket counts by state, and ephemeral port usage against the configured range.\n\nEphemeral exhaustion presents exactly like an upstream outage: new\nconnections are refused while established ones keep working. The\n`ephemeral.utilization` figure is what separates the two.\n\nNeeds hostNetwork to describe the node rather than the pod.\n",
   "get_system_snapshot": "One-shot node overview: cpu, memory, load, boot time, uptime, users.",
   "get_top_processes": "Top processes by 'cpu' or 'memory'. Needs hostPID to see node processes.",
   "read_text_head": "Read up to max_bytes of a text file under the readable-root allowlist (capped).",
   "stat_path": "Metadata (size, mode, mtime, type) for a path under the readable-root allowlist."
  }
 },
 "tailnet_coilyco_node_stats_ser8": {
  "description": "",
  "skill_description": "Use the tailnet_coilyco_node_stats_ser8 MCP for configured freshness, conntrack, cpu info, and deleted open files. Triggers - tailnet_coilyco_node_stats_ser8, tailnet_coilyco_node_stats_ser8 MCP",
  "source": "live",
  "tools": {
   "get_configured_freshness": "Freshness of server-configured host marker paths, without reading their content.",
   "get_conntrack": "Netfilter connection tracking: count against max, and the per-CPU error totals.\n\nRead from procfs rather than the conntrack binary, so this adds no\ndependency. `totals` carries every column the running kernel publishes,\nsummed across CPUs and parsed by column name, with insert_failed, drop and\nearly_drop being the ones an egress fault turns up in.\n\nThese are LIFETIME counters and only their movement is interpretable.\nNeeds the host /proc mount and hostNetwork to describe the node.\n",
   "get_cpu_info": "CPU utilization, core counts, and per-core percentages for this node.",
   "get_deleted_open_files": "Bounded deleted-open-file summary from fixed proc metadata.\n\nResults deduplicate inodes and distinguish disk-backed reclaimable files\nfrom memfd, tmpfs, devices, container overlays, and other non-disk entries.\nTarget paths are used only to match mount metadata and are never returned.\nTarget contents are never opened.\n",
   "get_disk_info": "Usage and mount info for the node's real filesystems (under ROOTFS).\n\n`options` carries the mount flags, which is where quota enforcement shows up\n(`prjquota`, `quota`, `usrquota`, `grpquota`) alongside `ro` and `noexec`.\nWithout it, two directories on one shared filesystem are indistinguishable\nfrom two filesystems with separate ceilings. See docs/tools-host.md.\n",
   "get_filesystem_pressure": "Root filesystem runway, inode use, and configurable pressure thresholds.\n\nThis is the fast disk-pressure view for the node root mounted at ROOTFS. The\ndefault thresholds are 80/85 percent because kubelet image garbage collection\nstarts to matter around that range on a single-filesystem k3s node.\n",
   "get_host_log_usage": "Bounded allocated usage for configured host log and journald roots.\n\nJournald roots nested beneath a configured log root are scanned separately\nand excluded from that parent, so aggregate bytes are not double-counted.\nCallers can cap returned child detail but cannot choose a path or widen the\nserver-owned traversal limits.\n",
   "get_host_usage_breakdown": "Cached mount-aware usage for one server-configured host profile.\n\nThe caller selects only a configured profile identifier. A missing, stale,\nor explicitly refreshed snapshot is scanned on a daemon worker thread while\nthis request returns current cache state immediately. Every snapshot labels\ncomplete totals versus lower bounds and reports filesystem and mount\nexclusions, including bind-mount deduplication.\n",
   "get_k3s_configured_conditions": "Conditions for server-configured Kubernetes custom-resource types.\n\nNarrow with namespace, an exact object name, or a configured source name, so\nlooking one ExternalSecret up does not page every one in the cluster.\n",
   "get_k3s_container_memory": "Approximate k3s container memory from metrics-server or host cgroup RSS.",
   "get_k3s_events": "Cluster events scoped to a namespace or one object, warnings first.\n\nThe object selector is the kubectl events --for shape: pass kind and name to\nget only the events attached to that object.\n",
   "get_k3s_logs": "Bounded, redacted container logs for one pod.\n\nprevious=True reads the container that died rather than the one that replaced\nit, which is where a crashloop explains itself. Output is capped at\nNODE_STATS_K3S_LOG_MAX_BYTES, truncation is reported rather than silent, and\nassignment-shaped secrets plus JWT, forge and AWS key literals are replaced\nbefore the text leaves the cluster. Redaction is a backstop, not a guarantee:\na workload that prints a credential in an unrecognised shape still prints it.\n",
   "get_k3s_namespaces": "Namespaces with phase, age, deletion timestamp, and finalizers.\n\nA namespace stuck Terminating is readable here: the finalizers holding it are\non the record rather than inferred from the phase.\n",
   "get_k3s_network": "Services, Ingresses and EndpointSlices for the routing path to a workload.\n\nEndpointSlices carry ready_endpoint_count, which is how a Service that\nresolves and answers nothing becomes visible. name also matches an\nEndpointSlice by the Service it backs.\n",
   "get_k3s_node_health": "Node conditions, taints, capacity, and bounded recent relevant events.",
   "get_k3s_pods": "Pods with container state, failure reason, exit code, and restart history.\n\nNarrow with namespace, an exact name, or a name prefix; the namespace filter\nis applied at the API, not after the fact. Each container carries state_detail\nand last_state, which name OOMKilled, ImagePullBackOff, CreateContainerConfigError\nand the exit code behind a crashloop. Init containers are listed separately.\nNot-Running pods sort first so a cap never truncates past the broken one.\n",
   "get_k3s_process_attribution": "Top host processes with cgroup-backed pod/container attribution when available.",
   "get_k3s_resource_usage": "Bounded node and pod usage from this node's kubelet Summary API.",
   "get_k3s_scheduled_work": "Bounded Jobs and CronJobs with failure, activity, and last-success timing.",
   "get_k3s_storage_claims": "PersistentVolumeClaims with phase, conditions, and their bound volume.\n\nThe lifecycle view beside get_k3s_volume_usage, which measures disk rather\nthan answering whether a claim bound. Unbound claims sort first.\n",
   "get_k3s_volume_usage": "Bounded local-volume disk usage joined to PVCs, PVs, namespaces, and pod mounts.\n\nThe server scans only local paths beneath NODE_STATS_K3S_VOLUME_ROOTS. Callers\ncan tune result and entry caps but cannot supply a filesystem path. The API\nreads and traversal run in a worker thread so fast node tools stay responsive.\n",
   "get_k3s_workloads": "Deployments, StatefulSets and DaemonSets with spec image and rollout state.\n\nspec_images is what the workload asks for, which is a different fact from the\nimage a running pod reports: they diverge exactly when a rollout was applied\nand has not landed. rollout_complete folds desired, ready, updated and\nobservedGeneration into the one answer. Incomplete rollouts sort first.\n",
   "get_memory_info": "Virtual and swap memory for this node (bytes and percent used).",
   "get_network_info": "Aggregate and per-interface network I/O counters for this node.\n\n`interfaces` takes \"default\" (drops per-pod veth churn), \"all\", or a\ncomma-separated list such as \"enp1s0,cni0,flannel.1,tailscale0\". What was\nfiltered is always reported, never silently dropped.\n\nThese are LIFETIME counters. A non-zero drop count says nothing on its own,\nbecause the node's uptime is days: only movement between two readings is\ninterpretable. See docs/tools-network.md.\n\nReflects the node only when the pod runs with hostNetwork. Otherwise these\nare the pod's own interface counters.\n",
   "get_node_pressure_stalls": "Linux PSI, selected VM-pressure counters, and bounded block-device I/O counters.",
   "get_pressure_path_usage": "Bounded child usage beneath fixed host paths that commonly drive pressure.\n\nTraversal runs in a worker thread, leaving the MCP event loop available for\nfast tools such as get_filesystem_pressure. Every configured root gets a\nbounded discovery slice, then every immediate child gets a fair entry and\ntime slice. Per-child results carry truncation, timeout, filesystem-skip,\npermission, and scan-error metadata. Nested configured roots are coalesced.\nThe caller can tune only the child entry cap and per-root result count,\nnever a raw path.\n",
   "get_resolver": "The node's DNS resolver, and optionally a pod's own as the container sees it.\n\nGive both `namespace` and `pod` to read that pod's resolv.conf through its\nhost PID. A container's resolver differs from its host's, and that\ndifference is what a DNS hypothesis turns on, so a mismatch in nameservers\nis stated in `notes` rather than left as two lists to diff.\n\nThe pod read needs hostPID and the pod running on this node. When it cannot\nbe satisfied the reason says which, rather than returning an empty result.\n",
   "get_socket_states": "TCP socket counts by state, and ephemeral port usage against the configured range.\n\nEphemeral exhaustion presents exactly like an upstream outage: new\nconnections are refused while established ones keep working. The\n`ephemeral.utilization` figure is what separates the two.\n\nNeeds hostNetwork to describe the node rather than the pod.\n",
   "get_system_snapshot": "One-shot node overview: cpu, memory, load, boot time, uptime, users.",
   "get_top_processes": "Top processes by 'cpu' or 'memory'. Needs hostPID to see node processes.",
   "read_text_head": "Read up to max_bytes of a text file under the readable-root allowlist (capped).",
   "stat_path": "Metadata (size, mode, mtime, type) for a path under the readable-root allowlist."
  }
 },
 "tailnet_coilyco_signoz": {
  "description": "This server exposes only policy-approved tools. Use read-only tools to inspect state before mutation tools. Follow each tool's input and output schema, and treat safety annotations as hints rather than authorization.\n\nLogs cover error and log searches like how many errors did X log today. Traces cover latency and request rates. Metrics cover trends and breakdowns over time. Alerts and dashboards cover firing alerts, alert history, dashboards, and saved views. This is SigNoz, the read-only fleet observability backend covering kai-server and ser8 for metrics, logs, traces, alerts, dashboards, saved views, and product docs. For right-now host or pod health snapshots, use node_stats.",
  "skill_description": "Use the tailnet_coilyco_signoz MCP for mcp beaver info, signoz aggregate logs, signoz aggregate traces. Triggers - tailnet_coilyco_signoz, tailnet_coilyco_signoz MCP",
  "source": "signoz 1c",
  "tools": {
   "mcp_beaver_info": "Report this MCP server's identity, mode, and served tool inventory. Read-only, reaches no upstream, and doubles as a liveness probe now that the protocol ping is gone.",
   "signoz_aggregate_logs": "Use this when the user wants aggregate statistics over logs\u2014counts, rates, averages, percentiles, or grouped/top-N breakdowns\u2014not individual log records. Use signoz_search_logs for log rows and message inspection; use signoz_execute_builder_query only for queries this tool cannot express. Log fields are workspace-specific, so read signoz://logs/query-builder-guide and discover unfamiliar keys with signoz_get_field_keys. Defaults to the last 1 hour.",
   "signoz_aggregate_traces": "Use this when the user wants custom aggregate statistics over spans\u2014counts, rates, latency percentiles, grouped/top-N breakdowns, or time series\u2014not individual span rows or a full trace hierarchy. For the built-in operation table for one traced service, ranked by p99, use signoz_get_service_top_operations. Use signoz_search_traces for raw spans or trace-ID discovery, and signoz_get_trace_details for one known trace ID. Before calling, read signoz://traces/query-builder-guide; discover unfamiliar workspace fields with signoz_get_field_keys. Defaults to the last 1 hour.",
   "signoz_check_metric_cardinality": "Use this when the user wants to find high-cardinality labels or attributes on one metric. It returns keys sorted by cardinality count with sample values, helping distinguish unbounded values such as UUIDs from bounded dimensions such as status codes. Do not use it to find dashboard or alert dependencies (signoz_check_metric_usage) or rank metric ingestion (signoz_get_top_metrics). This does not show whether the metric is used; check usage before recommending a drop.",
   "signoz_check_metric_usage": "Use this when the user needs to know which dashboards and alerts reference known metric names, especially before dropping or reducing telemetry. It returns dashboards, alerts, and an error for each metric, with up to 50 unique names per call. Do not use it for ingestion ranking (signoz_get_top_metrics) or label cardinality (signoz_check_metric_cardinality). A non-empty per-metric error means that entry is partial and unreliable; never interpret it as proof that the metric is unused.",
   "signoz_fetch_doc": "Use this after signoz_search_docs, or when an exact official SigNoz docs URL or /docs/... path is known, to return one page's full Markdown or a requested heading. Do not use it to discover pages or query live tenant data; use signoz_search_docs for topical discovery.",
   "signoz_get_alert": "Use this when the user wants one configured alert rule's full definition, or before signoz_update_alert so unchanged fields can be preserved. It requires a known rule ID; use signoz_list_alert_rules to discover IDs. Do not use it for current alert instances or firing history: use signoz_list_alerts or signoz_get_alert_history.",
   "signoz_get_alert_history": "Use this when the user wants alert firing history or the state-transition timeline of one configured rule; use signoz_list_alerts for current instances and signoz_get_alert for the rule definition. It requires a rule ID from signoz_list_alert_rules, defaults to the last 6 hours, and supports state/filter narrowing. For the next page, pass data.nextCursor as cursor and repeat the original filters, time range, and order.",
   "signoz_get_dashboard": "Use this when the user wants the complete definition of one known tenant dashboard, including its layout, variables, panels, and queries. Use signoz_list_dashboards first when the UUID is unknown. Do not use this to browse summaries or curated templates; use signoz_list_dashboards or signoz_list_dashboard_templates respectively.",
   "signoz_get_field_keys": "Use this when the user needs to discover field names available for filtering or grouping metrics, traces, or logs. It returns keys, not their observed values, scoped by signal and optional metric, context, or data type. After choosing a key, use signoz_get_field_values to discover valid values.",
   "signoz_get_field_values": "Use this when the user knows a field key and needs its observed values for a metrics, traces, or logs filter. It returns values, not field names; use signoz_get_field_keys when the key is unknown. Match signal and fieldContext to the query that will use the value.",
   "signoz_get_service_top_operations": "Use this when the user wants the built-in operation table for one traced service in a time window. It ranks operation names by p99 latency and returns p50, p95, p99, call count, and error count. Use signoz_list_services to discover active traced service names. For custom aggregation, grouping, time series, cross-service comparison, or arbitrary trace filters, use signoz_aggregate_traces instead. The optional tags parameter is a JSON-encoded TagQueryParam array.",
   "signoz_get_top_metrics": "Use this when the user wants to know which metrics drive ingestion volume or cost. It returns a fixed top 100 ranked by ingested sample count with pre-computed percentages for the requested window. Do not use it for metric values or trends (signoz_query_metrics), dashboard or alert dependencies (signoz_check_metric_usage), or label cardinality (signoz_check_metric_cardinality). This ranking has no offset pagination.",
   "signoz_get_trace_details": "Use this when the user already has a known trace ID and wants that trace's spans, metadata, and hierarchy. If the ID is unknown, discover it with signoz_search_traces first. Supply a time window containing the trace; the default last 6 hours can miss an older trace. Do not use this for filtering many spans or aggregate analysis.",
   "signoz_get_view": "Use this when the user wants the complete definition of one known saved Explorer view. Use signoz_list_views first when its UUID is unknown. The returned data object is the required base for signoz_update_view because updates fully replace a view. Do not use this for multi-widget dashboards; use signoz_get_dashboard.",
   "signoz_list_alert_rules": "Use this when the user wants configured alert-rule summaries, including inactive/OK and disabled rules. It returns rule IDs, names, types, state, severity, labels, and timestamps; use signoz_get_alert with an ID for the full definition. Do not use it for current firing/silenced/inhibited instances: use signoz_list_alerts. Paginate with limit and offset.",
   "signoz_list_alerts": "Use this when the user wants current firing, silenced, or inhibited Alertmanager alert instances and their state, severity, timing, and rule IDs. Do not use it for configured rules or history: use signoz_list_alert_rules for rule summaries, signoz_get_alert for one definition, or signoz_get_alert_history for its timeline. Filter by alert labels, state, or receiver before paginating.",
   "signoz_list_dashboard_templates": "Use this when the user wants to browse curated dashboard templates or discover a path for signoz_import_dashboard. It returns the complete bundled catalog with id, title, path, description, category, and keywords. It does not list dashboards already created in the tenant; use signoz_list_dashboards for those.",
   "signoz_list_dashboards": "Use this when the user wants to discover tenant dashboards, browse their summaries, or find a dashboard UUID. It returns names, descriptions, tags, timestamps, and a total count, not panel/query definitions; use signoz_get_dashboard for one full definition. Narrow the results with the optional filter expression (by name, description, tags, creator, timestamps, or locked state). When looking for a specific dashboard, page by raising offset by limit until you have covered total before concluding it is absent.",
   "signoz_list_metrics": "Use this when the user needs to discover metric names or inspect catalog metadata such as type, temporality, unit, and monotonicity. It lists metrics active in the requested window; searchText filters names by substring. Do not use it for metric values or trends\u2014use signoz_query_metrics, which can query a known exact name directly and auto-fetch missing metadata. Use source=\"meter\" only for Cost Meter metrics. Results have a limit but no offset pagination, so narrow the search if the cap is reached.",
   "signoz_list_notification_channels": "Use this when the user wants to discover configured notification channels, verify exact channel names before creating or updating an alert, avoid a duplicate name before channel creation, or find a channel ID. It returns paginated summaries only: id, name, type, and timestamps; it does not return provider-specific settings. Use signoz_get_notification_channel with an ID for all settings.",
   "signoz_list_services": "Use this when the user wants APM services with trace activity and their call or latency summaries in a time window. It returns paginated traced-service records; absence means no trace activity in that window, not that a matching service.name never appears in logs. For log values use signoz_get_field_values with signal=\"logs\" and name=\"service.name\"; for one service's operations use signoz_get_service_top_operations. Follow pagination.nextOffset until hasMore=false before concluding a traced service is absent.",
   "signoz_list_views": "Use this when the user wants to discover saved Explorer views or find a view UUID for one Logs, Traces, Metrics, or Cost Meter page. A view stores an Explorer query; it is not a multi-widget dashboard. Apply name/category filters before pagination, and follow pagination.nextOffset while pagination.hasMore is true before concluding a view is absent. Use signoz_get_view for one full definition.",
   "signoz_query_metrics": "Use this when the user wants metric values, trends, breakdowns, or formulas. It returns scalar or time-series results, applies metric-aware aggregation defaults, and reports the decisions used. If the exact metricName is known, call this directly: when metricType is omitted, type, temporality, and monotonicity are auto-fetched together. Use signoz_list_metrics only to discover names or inspect catalog metadata; use signoz_execute_builder_query for complex multi-query requests this tool cannot express. Standalone and formula results use top 100; formula inputs use 10000, and grouped time-series top-N is ranked over the whole window. Read signoz://metrics-aggregation-guide for rules and examples.",
   "signoz_search_docs": "Use this when the user asks a SigNoz product, setup, instrumentation, configuration, API, deployment, or troubleshooting question and no exact documentation page is selected. Returns ranked official-doc matches with URLs and snippets. Do not use for live tenant data; use signoz_fetch_doc when a result or exact docs URL needs full content.",
   "signoz_search_logs": "Use this when the user wants individual log records or messages matching text, service, severity, or field filters. It returns paginated rows, not counts, trends, or grouped breakdowns; use signoz_aggregate_logs for those, and signoz_execute_builder_query only for queries this tool cannot express. You do not need the guide when using only searchText, service, severity, time, or pagination. Read signoz://logs/query-builder-guide before filtering on unfamiliar fields. Defaults to the last 1 hour.",
   "signoz_search_traces": "Use this when the user wants individual raw span rows matching service, operation, error, duration, or field filters, or needs to discover trace IDs. It returns paginated spans, not aggregate trends/groups or a full trace hierarchy; use signoz_aggregate_traces for statistics and signoz_get_trace_details for one known trace ID. Read signoz://traces/query-builder-guide before using unfamiliar workspace fields. Defaults to the last 1 hour."
  }
 },
 "tailnet_coilyco_sirens_deep": {
  "description": "One conversational turn with Sirens Deep of Coilyco, a Discord community agent. Send a message and get the reply it would post: it reads its own knowledge and tools, and the answer passes the same response checks a member's would. Reach for it to ask a question in this community's voice, or to see what it would say. It is an agent rather than a data source, so it answers in prose rather than records, it may decline, and it is not a passthrough to the underlying model.",
  "skill_description": "Sirens Deep lane harness, as one `turn` tool. Same turn semantics as sirens-echo against Deep's own roster and identity. Triggers - tailnet_coilyco_sirens_deep, tailnet_coilyco_sirens_deep MCP",
  "source": "live",
  "tools": {
   "turn": "Answer one message through this deployment's policy, tools, and response validation."
  }
 },
 "tailnet_coilyco_sirens_echo": {
  "description": "One conversational turn with Sirens Echo of Coilyco, a Discord community agent. Send a message and get the reply it would post: it reads its own knowledge and tools, and the answer passes the same response checks a member's would. Reach for it to ask a question in this community's voice, or to see what it would say. It is an agent rather than a data source, so it answers in prose rather than records, it may decline, and it is not a passthrough to the underlying model.",
  "skill_description": "Sirens community lane harness, as one `turn` tool. A call runs a full agent turn, so it costs inference and is not a read. Triggers - tailnet_coilyco_sirens_echo, tailnet_coilyco_sirens_echo MCP",
  "source": "live",
  "tools": {
   "turn": "Answer one message through this deployment's policy, tools, and response validation."
  }
 },
 "tailnet_coilyco_skillsmp": {
  "description": "This server exposes only policy-approved tools. Use read-only tools to inspect state before mutation tools. Follow each tool's input and output schema, and treat safety annotations as hints rather than authorization.\n\nRead-only search over the skillsmp.com skill directory, which is one keyword-search endpoint.\nReach for it to find a published skill by keyword. It has no write or admin surface.",
  "skill_description": "Use the tailnet_coilyco_skillsmp MCP for mcp beaver info, and skills. Triggers - tailnet_coilyco_skillsmp, tailnet_coilyco_skillsmp MCP",
  "source": "live",
  "tools": {
   "mcp_beaver_info": "Report this MCP server's identity, mode, and served tool inventory. Read-only, reaches no upstream, and doubles as a liveness probe now that the protocol ping is gone.",
   "search_skills": "Use this when the user wants to search skills through the configured upstream service."
  }
 },
 "tailnet_coilyco_steam": {
  "description": "",
  "skill_description": "Use the tailnet_coilyco_steam MCP for account licenses, owned games, pics product info, and recently played. Triggers - tailnet_coilyco_steam, tailnet_coilyco_steam MCP",
  "source": "live",
  "tools": {
   "get_account_licenses": "Account-readable Steam package licenses, excluding access tokens.",
   "get_owned_games": "Kai's full owned Steam library, with app info and free games included.\n\nCalls `IPlayerService/GetOwnedGames` with `include_appinfo=1` (so names come\nback, not just appids) and `include_played_free_games=1`. This is the durable\nreplacement for the clipboard scrape's fields. Read-only.\n",
   "get_pics_product_info": "Authenticated PICS metadata for one app; refresh-token first, read-only.",
   "get_recently_played": "Games Kai has played in the last two weeks - a recent-activity signal.\n\nCalls `IPlayerService/GetRecentlyPlayedGames`. Cheap and useful alongside the\nfull library; the same normalized record shape. Read-only.\n",
   "get_store_app_details": "Public metadata for one app via the unauthenticated Steam storefront.\n\nThis is not a Web API or account-client credential path.  It is a fixed,\nread-only storefront endpoint, intentionally without any account cookie.\n",
   "get_store_search_results": "Public Steam storefront search; returns a small normalized result set."
  }
 },
 "tailnet_coilyco_teable": {
  "description": "This server exposes only policy-approved tools. Use read-only tools to inspect state before mutation tools. Follow each tool's input and output schema, and treat safety annotations as hints rather than authorization.\n\nIssue tracker records in the tailnet Teable: list and read rows, file new ones, update one row.\nRecords only. It mounts no schema write, because this instance applies field writes unreliably.\nA 2xx here is not proof: read the record back before reporting a write landed.",
  "skill_description": "Records-only surface over the tailnet Teable issue tracker. It mounts no schema write, and a 2xx is not proof a write applied, so read... Triggers - tailnet_coilyco_teable, tailnet_coilyco_teable MCP",
  "source": "live",
  "tools": {
   "convert_table-field": "NOT AVAILABLE - this operation is intentionally withheld on this surface. IRREVERSIBLE. A convert emptied all 6,536 values in the column it declared required, returning 200 with notNull true (deploy#850 defect 5). It is the one verb that destroys data while reporting the opposite. Do it in the Teable UI on the tailnet with an export in hand, and read the column back before trusting the response. Use list_table-field instead. Calling this tool always fails and reaches no upstream.",
   "create_record": "Use this when the user wants to create record through the configured upstream service.",
   "create_table-field": "NOT AVAILABLE - this operation is intentionally withheld on this surface. Teable accepts unknown field properties and silently discards them (deploy#850 defect 1), so a schema this surface appeared to create would not be the schema stored. Create fields in the Teable UI on the tailnet, then read back what was actually stored. Use list_table-field instead. Calling this tool always fails and reaches no upstream.",
   "delete_record": "NOT AVAILABLE - this operation is intentionally withheld on this surface. Teable has no archive verb for a record, so a delete is unrecoverable outside a restic PVC restore. This surface is additive and corrective only: correct a row in place, or set its state field to closed. Use edit_record instead. Calling this tool always fails and reaches no upstream.",
   "edit_record": "Use this when the user wants to edit record through the configured upstream service.",
   "edit_table-field": "NOT AVAILABLE - this operation is intentionally withheld on this surface. PATCH on a field returns 200, validates nothing and applies nothing (deploy#850 defect 3). A formula can be corrected, report success, and keep running the old logic. Convert in the Teable UI on the tailnet, then confirm what was stored. Use list_table-field instead. Calling this tool always fails and reaches no upstream.",
   "get_record": "Use this when the user wants to get record through the configured upstream service.",
   "list_base": "Use this when the user wants to list base through the configured upstream service.",
   "list_base-table": "Use this when the user wants to list base-table through the configured upstream service.",
   "list_record": "List records from one table. `search` is the ONLY filter that reaches this surface, and it is a three-element tuple [term, fieldId, \"true\"]. The third element MUST be the string \"true\" to filter. Any other value, and the two-element form, return the unfiltered head of the table, indistinguishable from a filter that matched everything. searchHitIndex does not catch that: it is populated only when the searched field is among the returned fields, so any projection omitting it reads null on a filter that ran correctly. Negative-control with a nonsense term that must return zero rows, or put the searched field in the projection. Matching is case-insensitive substring scoped to the single field named by fieldId, which comes from list_table-field. `filter`, `filterByTql` and `orderBy` take JSON or TQL braces, and reach upstream only on an image built against umbra v0.196.0 or later, where the metachar gate stopped rejecting `{` in query values. An older image refuses them with a policy error naming the brace (mcp-beaver#6930). In TQL, LIKE is EXACT EQUALITY: neither % nor * is a wildcard, so `{title} LIKE \"%term%\"` returns zero rows with a 200 and no warning. Use `search` for substring matching and `filterByTql` for equality, comparison and boolean composition. Pass projection to bound the payload, and page with take and skip.",
   "list_table-field": "Use this when the user wants to list table-field through the configured upstream service.",
   "list_table-view": "Use this when the user wants to list table-view through the configured upstream service.",
   "mcp_beaver_info": "Report this MCP server's identity, mode, and served tool inventory. Read-only, reaches no upstream, and doubles as a liveness probe now that the protocol ping is gone."
  }
 },
 "tailnet_coilyco_trello": {
  "description": "This server exposes only policy-approved tools. Use read-only tools to inspect state before mutation tools. Follow each tool's input and output schema, and treat safety annotations as hints rather than authorization.\n\nBounded Trello board workflow: boards, lists, cards, labels, checklists, actions and URL attachments.\nIt omits webhooks, token management and organization administration.\nPrefer archiving over deletion: closed=true is reversible, and hard delete exists only where Trello offers no archive verb.",
  "skill_description": "Use the tailnet_coilyco_trello MCP for card label, card, board label, and card attachment. Triggers - tailnet_coilyco_trello, tailnet_coilyco_trello MCP",
  "source": "live",
  "tools": {
   "add_card-label": "Use this when the user wants to add card-label through the configured upstream service.",
   "comment_card": "Use this when the user wants to comment card through the configured upstream service.",
   "create_board-label": "Use this when the user wants to create board-label through the configured upstream service.",
   "create_card": "Use this when the user wants to create card through the configured upstream service.",
   "create_card-attachment": "Use this when the user wants to create card-attachment through the configured upstream service.",
   "create_card-checklist": "Use this when the user wants to create card-checklist through the configured upstream service.",
   "create_checklist-item": "Use this when the user wants to create checklist-item through the configured upstream service.",
   "create_list": "Use this when the user wants to create list through the configured upstream service.",
   "delete_board-label": "IRREVERSIBLE. Deletes the label everywhere, stripping it from every card that carries it across the whole board. To take a label off one card, use remove_card_label instead.",
   "delete_card-attachment": "IRREVERSIBLE. Trello has no archived state for an attachment, so this is the only way to take one off a card.",
   "delete_checklist": "IRREVERSIBLE. Removes the checklist and every item on it. Checklists have no archived state.",
   "delete_checklist-item": "IRREVERSIBLE. Check items have no archived state. To mark one done instead, use set_card_checkitem with state=complete.",
   "get_board": "Use this when the user wants to get board through the configured upstream service.",
   "get_card": "Use this when the user wants to get card through the configured upstream service.",
   "get_checklist": "Use this when the user wants to get checklist through the configured upstream service.",
   "get_list": "Use this when the user wants to get list through the configured upstream service.",
   "list_board-card": "Use this when the user wants to list board-card through the configured upstream service.",
   "list_board-checklist": "Use this when the user wants to list board-checklist through the configured upstream service.",
   "list_board-label": "Use this when the user wants to list board-label through the configured upstream service.",
   "list_board-list": "Use this when the user wants to list board-list through the configured upstream service.",
   "list_board-member": "Use this when the user wants to list board-member through the configured upstream service.",
   "list_card-action": "Use this when the user wants to list card-action through the configured upstream service.",
   "list_card-attachment": "Use this when the user wants to list card-attachment through the configured upstream service.",
   "list_card-checklist": "Use this when the user wants to list card-checklist through the configured upstream service.",
   "list_card-custom-field": "Use this when the user wants to list card-custom-field through the configured upstream service.",
   "list_card-member": "Use this when the user wants to list card-member through the configured upstream service.",
   "list_checklist-item": "Use this when the user wants to list checklist-item through the configured upstream service.",
   "list_list-card": "Use this when the user wants to list list-card through the configured upstream service.",
   "list_my-board": "Use this when the user wants to list my-board through the configured upstream service.",
   "mcp_beaver_info": "Report this MCP server's identity, mode, and served tool inventory. Read-only, reaches no upstream, and doubles as a liveness probe now that the protocol ping is gone.",
   "remove_card-label": "Use this when the user wants to remove card-label through the configured upstream service.",
   "search_trello": "Use this when the user wants to search trello through the configured upstream service.",
   "set_board": "Use this when the user wants to set board through the configured upstream service.",
   "set_card": "Use this when the user wants to set card through the configured upstream service.",
   "set_card-checkitem": "Use this when the user wants to set card-checkitem through the configured upstream service.",
   "set_checklist": "Use this when the user wants to set checklist through the configured upstream service.",
   "set_list": "Use this when the user wants to set list through the configured upstream service."
  }
 }
}
